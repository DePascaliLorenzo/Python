Librerie installate:
- Pandas (creazione e gestione di dataframe) -> pip install pandas
- Matplotlib (creazione di grafici di base) -> pib install matplotlib
- Scipy (calcoli scientifici e statistici) -> pip install scipy
- Scikit Learn (preprocessing e modelli ML) -> pip install scikit-learn
- Seaborn (funzionalità grafiche e dataset -> pip install seaborn
- Kneed (individuazione numero cluster) -> pip install kneed